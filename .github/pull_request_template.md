## Change

What changed, and why?

## Verification

- [ ] Full tests passed (`swift test --build-system native`).
- [ ] Release packaging passed (`bash scripts/build-app.sh release`).
- [ ] UI changes checked in the packaged app, where applicable.
- [ ] Repository mutations tested with staged/unstaged data in disposable repos.
- [ ] No credentials, private data, generated bundles, or unrelated changes added.

Known limits or checks not performed:
